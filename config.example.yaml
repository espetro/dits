# Example config for local development with the in-repo mock provider.
# Start the mock provider first: bun run packages/evals/mock-provider/main.ts --port 9000

server:
  port: 3000
  auth: none

llm:
  provider: mock
  base_url: http://localhost:9000/v1
  model: mock-llm

stt:
  base_url: http://localhost:9000/v1
  model: mock-stt
  mode: buffered

tts:
  base_url: http://localhost:9000/v1
  model: mock-tts
  voice: alloy

# optional: enables document ingestion (RAG). omit to disable file uploads.
embeddings:
  base_url: http://localhost:9000/v1
  model: mock-embed

# optional: paths for the sqlite db, log and data dir. omit `files` entirely
# (or any single key) to use the platform app-support dir —
# ~/Library/Application Support/di, %APPDATA%/di, $XDG_DATA_HOME/di or
# ~/.local/share/di — which is what the desktop sidecar uses by default.
files:
  db_path: .deepinterview/di.db
  log_path: .deepinterview/di.log
  data_dir: .deepinterview/data
